---
last_reviewed: 2026-05-07
reflects_commit: 5b9ac5a
---

# Phase 7 — Polish backlog (post-doc refactor)

> **Resume from this file** when picking work up on any PC. Every
> task carries a checkbox, a 1-line description, and the page or
> file that needs touching. Work P1 → P2 → P3. Bump
> `last_reviewed` + `reflects_commit` when ticking a row.

Scope summary:

- **P1 — mdBook documentation polish.** Navigation, broken links,
  template uniformity, Mermaid syntax fixes. Pure docs work.
- **P2 — Per-package code improvements.** Concrete TODOs surfaced
  while reading every package's tech md. Each item is a small
  feature, hardening pass, or E2E test gap.
- **P3 — Ideas / new packages.** Bigger chantiers (custom packer,
  Red-Pill family of VM detection, post-evolution worked examples).

## Ordering directive (set 2026-04-29)

Work the rows in the following ranking — **do NOT round-robin
across bands**:

1. **Finish every P1 row first.** Pure docs work, fastest visible
   wins (live mdBook is the user-facing artefact). All eight
   P1.x rows close before any P2 / P3 row starts.
2. **Then quick wins from P2/P3.** Rows tagged `🚀 quick-win`
   below — small, scoped, directly useful. Order them by
   "smallest diff" first.
3. **Then high-impact rows from P2/P3.** Rows tagged
   `⭐ high-impact` — bigger diffs that unlock material
   capability gains (e.g. RTCore64 E2E, sandbox scoring,
   API hashing extensions, syscall package separation).
4. **Then everything else** in P-section / row-number order.

Tags below are advisory — adjust freely as rows ship and the
remaining picture changes.

### Concrete order

**Band 0 — finish P1 (9 rows, in number order):**

P1.1 ✅ → P1.2 ✅ → P1.3 🟡 → P1.4 ✅ → P1.5 ✅ → P1.6 ✅ → P1.7 ✅ → P1.8 ✅ → P1.9 ✅

P1.3 is the remaining mega-sweep: 62 tech md pages still render
API Reference as prose / single code block instead of the
canonical per-export fielded format. **Per-area, per-commit** —
see the "Status" table inside row P1.3 for current coverage.
Rows in Band-1 / Band-2 may proceed in parallel because P1.3 is
genuinely long-tail.

**Band 1 — `🚀 quick-win` (small diff, immediate user value):**

| Row | Why a quick win |
|---|---|
| P2.11 — RTCore64 E2E (1 row remaining) | Embed already done; E2E is one VM test. |
| P2.22 — KNOWNFOLDERID upgrade | Single helper + repo-wide search-and-replace. |
| P2.4 (md-only parts) — SAM dump scope notes | Markdown additions only. |
| P2.3 (md-only parts) — Sekurlsa scope notes | Markdown additions only. |
| P2.5 (md-only parts) — Golden ticket scope notes | Markdown additions only. |
| P2.15 — Service account override | Single builder option + LSA grant. |
| P2.7 — Preset stacks review | Audit + add Stealth-with-IndirectAsm preset. |
| P2.21 — Drive event-driven Watch | One new file + `WM_DEVICECHANGE` plumbing. |
| P2.19 — Phant0m heartbeat re-kill | Single ticker goroutine. |
| P2.8 / P2.10 — CET prefix on Thread Pool / Callback Execution | One-byte payload prefix. |

**Band 2 — `⭐ high-impact` (big capability or composability gain):**

| Row | Why high impact |
|---|---|
| P2.25 — Syscall package documentation clarity | Most-imported area, composability hinges on it. |
| P2.24 — API hashing algorithm extensions | Composes throughout the library. |
| P2.20 — Sandbox detection scoring algorithm | Operator-visible behaviour change. |
| P2.16 — LNK fileless + stealthopen | New operational mode. |
| P2.18 — HideProcess broader patches | Closes the "still visible via WMI" gap. |
| P2.23 — BOF Loader Beacon API + relocations | Material capability boost. |
| P3.5 — Refresh worked examples | Highest reader value of P3. |
| P2.1 — Crypto hardening (entropy + wipe + streaming) | Touches every payload pipeline. |
| P2.12 — Masquerade presets | Catalog expansion. |

**Band 3 — rest** (P2.2 LSASS alt-drivers research, P2.6 callstack HW-BP,
P2.9 Phantom DLL clarification + E2E, P2.13 PE cert pure-Go forge, P2.14
DLL proxy CheckSum/DOS, P2.17 Session desktop, P3.1 custom packer, P3.2
Red Pill family, P3.3 CPUID/RDTSC, P3.4 VMware port).

Process per task:

1. Tick the checkbox `[x]` when shipped.
2. Note the commit SHA in the **Done** column.
3. Bump the front-matter `last_reviewed` + `reflects_commit` on
   each commit that completes a row.

---

## P1 — mdBook documentation polish

### P1.1 — Collapse `Techniques` sections by default in the SUMMARY

The current `docs/SUMMARY.md` lists every per-technique page under
each area as a child bullet — mdBook renders all of them expanded
in the sidebar, which buries the lower areas.

Root cause: mdBook's fold check is `section_number.len() > level`
(strict `>`). With the previous `level = 1`, chapters at depth 1
were NOT folded (`1 > 1` false) so the area pages rendered
expanded. Bumping to `level = 0` (strict `>`) folds every
chapter at depth ≥ 1 that has sub-items — the Techniques area
pages — while childless top-level entries (Getting Started, By
Role, Reference) stay visible because mdBook's fold path requires
`has_sub_items` to be true.

No SUMMARY restructure needed — the existing tree already has the
right shape; only the threshold was wrong.

| | Action | Where | Done |
|---|---|---|---|
| [x] | Diagnose: live HTML shows `class="chapter-item expanded"` on every area page; CSS hides children only when parent lacks `.expanded` | `chrome.css` rule `.chapter li:not(.expanded) + li > ol { display: none }` | this commit |
| [x] | Bump fold threshold to `level = 0` (strict `>`) | `book.toml` | this commit |
| [ ] | Verify post-deploy that area pages render collapsed | live mdBook | post-deploy |

### P1.2 — Fix 404 on Package column in tech md tables

Every area README has a "Packages" table whose first column links
to `../../<pkg>/` (pkg.go.dev style). On the deployed mdBook
those resolve to `https://oioio-space.github.io/maldev/<pkg>/`
which is a 404 — the docs site only contains `docs/`, not the
source tree.

| | Action | Where | Done |
|---|---|---|---|
| [x] | Sweep every `docs/techniques/**/*.md` for `(../../../<pkg>)` patterns (105 hits across 30 pages) | repo-wide sed | this commit |
| [x] | Replace with absolute pkg.go.dev URLs | per-page rewrite | this commit |
| [x] | Update `cmd/docgen` MITRE renderers to emit absolute pkg.go.dev URLs natively (autogen index table no longer 404s either) | `cmd/docgen/main.go` + new test `TestRenderMITRE_AbsolutePkgGoDevURLs` | this commit |
| [ ] | Confirm with the live-link checker after next mdBook deploy | `curl` smoke test | post-deploy |

### P1.3 — Unify API Reference template across every tech md

✅ **Closed 2026-05-04.** All 19 area rows fielded across the
sweep series `4236b30..7a8c466` (commit-per-area, one per
commit); every tech md page with an `## API Reference` heading
now uses the canonical per-export fielded format (heading +
Parameters / Returns / Side effects / OPSEC + Required
privileges / Platform). Original audit baseline (2026-04-29):
27/89 fielded. Final: 89/89.

Strategy: process one area per commit so any cross-PC resume
picks up exactly where the last commit left off.

| Area | Total pages | Already fielded | Pending | Status |
|---|---|---|---|---|
| `recon/*`       | 9 | 9 | 0 | ✅ |
| `pe/*`          | 8 | 8 | 0 | ✅ |
| `process/*`     | 7 | 7 | 0 | ✅ |
| `persistence/*` | 7 | 7 | 0 | ✅ |
| `evasion/*`     | 14 | 14 | 0 | ✅ |
| `c2/*`          | 7 | 7 | 0 | ✅ |
| `injection/*`   | 13 | 13 | 0 | ✅ |
| `credentials/*` | 5 | 5 | 0 | ✅ |
| `tokens/*`      | 4 | 4 | 0 | ✅ |
| `syscalls/*`    | 4 | 4 | 0 | ✅ |
| `collection/*`  | 6 | 6 | 0 | ✅ |
| `runtime/*`     | 3 | 3 | 0 | ✅ |
| `hash/*`        | 3 | 3 | 0 | ✅ |
| `crypto/*`      | 2 | 2 | 0 | ✅ |
| `encode/*`      | 2 | 2 | 0 | ✅ |
| `kernel/*`      | 2 | 2 | 0 | ✅ |
| `win/*`         | 3 | 3 | 0 | ✅ |
| `privesc/*`     | 3 | 3 | 0 | ✅ |
| `cleanup/*`     | 8 | 8 | 0 | ✅ |

For each pending page:

1. Convert each export listing to a per-export `### `Foo(args)
   (R, error)`` heading.
2. Add the bulleted fields below the heading: godoc / Description /
   Parameters / Returns / Side effects / OPSEC / Required
   privileges / Platform.
3. Bump page front-matter `last_reviewed` + `reflects_commit`.
4. Tick the area in the table when its row reaches 100% fielded.

Skeleton lives in `docs/conventions/documentation.md`.

#### Follow-up items surfaced during the P1.3 sweep

These were discovered while reading the source against the
legacy doc. They are out-of-scope for the API-Reference sweep
(touch sections that were explicitly held untouched, or are
code-vs-doc decisions) and are tracked here for separate
follow-up:

| | Item | Where | Decision needed |
|---|---|---|---|
| [x] | `task-scheduler.md` API Reference cleaned: dropped the stale meta-comment about a TL;DR "recursive sub-folders" claim that no longer exists; replaced with a constructive note pointing operators at `ITaskFolder.GetFolders` for sub-folder walks and flagging `ListAll()` as a candidate enhancement. TL;DR + How-It-Works already accurate. Code unchanged (root-only enumeration is correct as documented). | `docs/techniques/persistence/task-scheduler.md` | this commit |
| [x] | `recon/sandbox/doc.go` gains a "Re-exported primitives" section listing the three convenience wrappers (`Checker.IsDebuggerPresent` / `IsRunningInVM` / `BusyWait`) and explaining that they exist for ergonomics only — `recon/antidebug`, `recon/antivm`, `recon/timing` remain the canonical entry points. `BusyWait` is the only one carrying state binding (Config.EvasionTimeout becomes the duration). | `recon/sandbox/doc.go` | this commit |
| [x] | Repo-wide grep for `MachineI386` + `Phase 3` shows no stale claims in user-facing surfaces (README, mitre.md, tech mds, examples). The only remaining `Phase 3` references live in internal plan files (`.dev/superpowers/plans/2026-04-28-pe-dllproxy.md`) and mark the work as ✅ done. No edit needed. | repo-wide grep | this commit |



`docs/techniques/collection/*` already has a "Cross-references"
section near the bottom that points at sibling/composing
techniques. It was missing on 21 other tech md pages.

| | Action | Where | Done |
|---|---|---|---|
| [x] | Audit: 21 tech md files lacked `## See also` | repo-wide grep | this commit |
| [x] | Curate per-page cross-refs (2-4 siblings + composer + area README) | per-page Edit | this commit |
| [x] | Verify post-sweep: 0 tech md files missing `## See also` | repo-wide grep | this commit |

### P1.5 — Quick decision tree on every area README

Some area READMEs (recon, evasion, c2) already had a
**Decision tree** section. Three were missing a quick-decision
table:

| | Action | Where | Done |
|---|---|---|---|
| [x] | Audit: 3 area READMEs missing a decision-tree table (`injection`, `syscalls`, `tokens`) | `docs/techniques/*/README.md` | this commit |
| [x] | Add a 8-12 row "operator question → use" table to each | per-README Edit | this commit |

### P1.6 — Payload encryption comparison table

`docs/techniques/crypto/payload-encryption.md` documented AES-GCM,
XChaCha20-Poly1305, RC4, XOR, TEA, XTEA, S-Box, ArithShift,
MatrixTransform in prose. Added a side-by-side near the top.

| | Action | Where | Done |
|---|---|---|---|
| [x] | Author "Pick the primitive" 9-row matrix (Layer / Speed / Entropy / Key / IV / Authenticated / Reversible / Static signature / Best-for) + composition pattern + AEAD-outer rule | `docs/techniques/crypto/payload-encryption.md` § "Pick the primitive" | this commit |
| [x] | Cross-link the new matrix from `crypto/README.md` — added a paragraph in `docs/techniques/crypto/README.md` (the area README, since the package itself has no README.md) pointing readers at the 9-row "Pick the primitive" matrix as the canonical decision surface; the existing Quick decision tree is now framed as a shortcut. | `docs/techniques/crypto/README.md` | this commit |

### P1.7 — Bring two pages to canonical template + add new fields

| | Action | Where | Done |
|---|---|---|---|
| [x] | Add `Required privileges` + `Platform` fields to canonical API Reference template (closed-set levels: unprivileged / medium-IL / admin / SYSTEM / kernel) | `docs/conventions/documentation.md` | `b81bd80` |
| [x] | Rewrite `callstack-spoof.md` to template (TL;DR / Primer / How It Works / API Reference fielded / Examples Simple+Composed+Advanced / OPSEC & Detection / MITRE / Limitations / See also) | `docs/techniques/evasion/callstack-spoof.md` | `5e0e5c2` |
| [x] | Rewrite `kernel-callback-removal.md` to template; one fielded entry per export (NtoskrnlBase / DriverAt / Enumerate / Remove / Restore) with the new privilege + platform fields | `docs/techniques/evasion/kernel-callback-removal.md` | this commit |
| [x] | Backfill `Required privileges` + `Platform` on every other tech md — repo-wide grep confirms 0 tech md pages remain without the two new fields. The P1.3 sweep landed the fields on the 62 pages it rewrote; this row covered the residual ~28 pages that were already fielded under the older template (cleanup/*, collection/*, evasion/* legacy fielded, injection/*, pe/* recently-touched, privesc/*, recon/sandbox). | repo-wide grep + per-page edits | 2026-05-04 |
| [x] | Backfill equivalent on every `doc.go` — all 97 doc.go pages now carry `# Required privileges` + `# Platform` headings (operator-relevant, per-export when behaviour varies). Sweep landed area-by-area across commits `b328731..72d04f8` (c2 → cleanup → collection → credentials → layer-0 → persistence → pe → win → process → recon → evasion → final 9). | `*/doc.go` | 2026-05-04 |

### P1.9 — Remove legacy area pages from book

The mdBook SUMMARY's "Area Overviews (legacy)" section listed
flat `docs/<area>.md` files (15 total). Each was superseded by
the per-area `docs/techniques/<area>/README.md` page.

| | Action | Where | Done |
|---|---|---|---|
| [x] | Audit inbound references to each legacy file (29 unique refs across 11 files; 4 of them in scratch dirs ignored) | repo-wide grep | this commit |
| [x] | Rewrite the live refs (README.md privesc row, persistence/account+README, privesc/README, sekurlsa breadcrumb + inline note, evasion/preset + stealthopen API-Reference stub, architecture.md / getting-started.md table cells) | per-file Edit / sed | this commit |
| [x] | Drop the 4 "flat API reference (legacy …)" footnote bullets in cleanup, credentials, evasion, runtime area READMEs | sed delete | this commit |
| [x] | Drop the "Area Overviews (legacy)" section from `docs/SUMMARY.md` | `docs/SUMMARY.md` | this commit |
| [x] | `git rm` the 15 legacy `docs/<area>.md` files (~9 800 lines purged) | `git rm` | this commit |
| [x] | Repo-wide broken-link sweep: 0 broken refs after the rewrites | shell broken-link checker | this commit |
| [ ] | Verify mdBook deploy: 0 dead links, sidebar shorter | live site | post-deploy |

### P1.8 — Mermaid v11 syntax fixes

mdbook-mermaid bumped to v11 and these pages rendered with a
visible "syntax error" banner.

| | Action | Notes | Done |
|---|---|---|---|
| [x] | Bulk sweep `<br/>` → `<br>` across 197 occurrences in 59 files | Mermaid v11 dropped XHTML self-close support; `<br>` is the v11 form | this commit |
| [x] | `crypto/payload-encryption.md` — strip square-bracket `[32 bytes]` / `[12 bytes]` / `[:12]` from sequence-diagram message text; rewrite `EMBED[//go:embed in implant]` to quoted text | sequence-diagram messages and bracketed flow nodes choke on bare `[` | this commit |
| [x] | `evasion/cet.md` — quote node labels containing `()` / `=` / `?` (`Q{cet.Enforced()?}` → `Q{"cet.Enforced ?"}`) | unquoted `()` inside `{}` decision nodes fails | this commit |
| [x] | `evasion/inline-hook.md` — replace `(±2GB)` in participant alias with ASCII `within 2GB` | `±` and other non-ASCII in participant aliases not accepted | this commit |
| [x] | `pe/imports.md` — quote every node label; replace `{parse "Func:DLL"}` → `{"parse Func then DLL"}` | nested quotes inside `{}` confuse the parser | this commit |
| [x] | `pe/certificate-theft.md` — drop `[4]` / `(parens)` / `;` from sequence Note text; ASCII-ize | sequence Notes prefer plain ASCII | this commit |
| [x] | `persistence/lnk.md` — rewrite `PutProperty(... / ... / ...)` message text without `(` `/` so sequence parser stops choking | `(/)` in sequence message body misparses | this commit |
| [x] | `recon/hw-breakpoints.md` — quote labels; replace `{TID, Register, Address}` (nested braces) with plain text | nested `{}` inside `[]` rejected | this commit |
| [x] | `recon/dll-hijack.md` — quote labels; replace `[]Opportunity` (looks like a Mermaid node) with plain text | bare `[...]` mid-label parsed as node | this commit |
| [x] | `evasion/amsi-bypass.md` + `evasion/etw-patching.md` | covered by the bulk `<br>` sweep — no other broken construct | this commit |

---

## P2 — Per-package code improvements

### P2.1 — Payload encryption hardening

| | Action | Where |
|---|---|---|
| [x] | Document entropy concern + recommend layered transforms (ArithShift) — `crypto/doc.go` gains an "Entropy + layering" section explaining why AEAD output is YARA-friendly on its own and recommending the AES → MatrixTransform → ArithShift → SBox stack. The tech md Limitations bullet (line ~505) already covered the recommendation; the godoc surface now matches. | `crypto/doc.go` + tech md (already in place) | this commit |
| [x] | Add ephemeral-plaintext wipe helper that zeroes the buffer right after `Inject` consumes it — `crypto.Wipe(buf)` (re-export of `cleanup/memory.SecureZero` so callers don't need a second import) and `crypto.UseDecrypted(decrypt, fn)` which runs `decrypt`, hands the plaintext to `fn`, and `defer`-wipes — wipe still runs when `fn` errors or panics. 6 unit tests covering happy path, decrypt error, fn error (still wipes), wipe-after-return observability, nil/empty-buffer guard. | `crypto/wipe.go` + `crypto/wipe_test.go` (new) | this commit |
| [x] | Streaming API for multi-MB payloads (`io.Reader`/`io.Writer` flavour of EncryptAESGCM, ChaCha20) — `NewAESGCMWriter` / `NewAESGCMReader` / `NewChaCha20Writer` / `NewChaCha20Reader` wrap any `io.Writer` / `io.Reader` and frame AEAD output into 64 KiB plaintext chunks. Frame: `[4-byte BE header (final bit + length)][sealed bytes]`. Per-frame AAD `[8-byte counter || 1-byte final]` authenticates ordering + EOS marker — truncation surfaces as `ErrStreamTruncated`. Bounded peak memory + zero per-frame allocations (nonce/AAD/Seal/Open buffers reused on the struct). DoS-guard caps reader's per-frame `make` to `streamChunkSize + aead.Overhead()`. 8 tests × 2 factories = 16 sub-tests. /simplify pass applied (per-frame allocs eliminated, `errors.Is`, body-truncation discrimination, writer-poisoning on flush error). | `crypto/{stream.go,stream_test.go}` (new) | this commit |
| [x] | Note `MatrixTransform` per-row allocation cost in API Reference — added a callout under the `MatrixTransform` API entry quantifying the per-block row-vector allocation (262 144 transient allocs for a 1 MiB payload at n=4) and recommending AES/ChaCha as the outer envelope when allocator pressure matters. | `docs/techniques/crypto/payload-encryption.md` | this commit |

### P2.2 — LSASS dump alternative drivers

| | Action | Where |
|---|---|---|
| [ ] | Investigate alternative BYOVD drivers usable for PPL bypass (replace dependency on RTCore64) | `credentials/lsassdump/` |
| [ ] | Evaluate features from impacket / Mandiant-published tooling worth porting to pure Go | research note in `docs/techniques/credentials/lsassdump.md` |

### P2.3 — Sekurlsa parser

| | Action | Where | Done |
|---|---|---|---|
| [x] | Credential Guard / LSAISO surfaced via `Result.Warnings` — confirmed in tech md Limitations | `docs/techniques/credentials/sekurlsa.md` | already in place |
| [x] | "No live-process attach" gap documented in Limitations with pointer to future `credentials/lsalive` chantier | tech md Limitations | already in place |
| [ ] | Mine impacket / Mandiant write-ups for missing credential providers (e.g., Web Credentials) | research note | — |

### P2.4 — SAM dump

| | Action | Where | Done |
|---|---|---|---|
| [x] | Add NT-history (password-history) parsing — V-region tail bytes. New `Account.NTHistory` / `Account.LMHistory [][]byte` fields populated from V offsets 0xB4/0xB8 (NTHist) + 0xC0/0xC4 (LMHist), decrypted through both legacy (RC4) and modern (AES-CBC) wrappers via new `decryptUserNTHistory` / `decryptUserLMHistory` helpers. New `Account.PwdumpHistory()` + `Result.PwdumpWithHistory()` render `<user>_history<N>:RID:LM:NT:::` lines à la impacket. 11 new tests (parse + roundtrip per envelope + render). | `credentials/samdump/{users,decrypt,samdump}.go` + tests + tech md Limitations rewrite + new "with history" Example | this commit |
| [x] | Local-only scope (NTDS.dit out of scope) + DPAPI / SECURITY exclusion documented | `docs/techniques/credentials/samdump.md` Limitations | already in place |
| [x] | Live-mode noise (`reg.exe save`) cited under Limitations | tech md Limitations | already in place |
| [ ] | Mine impacket secretsdump for any missing field | research note | — |

### P2.5 — Golden ticket

| | Action | Where | Done |
|---|---|---|---|
| [x] | krbtgt-rotation behaviour documented | `docs/techniques/credentials/goldenticket.md` Limitations | already in place |
| [x] | Diamond / Sapphire / Silver out-of-scope notes | tech md Limitations | already in place |
| [x] | Submit's per-process / per-LUID effect documented | tech md Limitations | already in place |
| [x] | Add a PAC validation guard — `ValidatePAC(pacBytes, h)` mirrors MS-PAC §2.8 in reverse: parse → locate sig buffers → save sigs → zero them → recompute server checksum over zeroed bytes → recompute KDC checksum over saved server sig → constant-time compare via `hmac.Equal`. 4 sentinel errors (`ErrPACMissing{Server,KDC}Signature`, `ErrInvalid{Server,KDC}Signature`) + 9 tests (round-trip RC4/AES128/AES256, surgical buffer-tampering via `tamperSignatureByte` helper that uses the PAC's own Buffers metadata, wrong-key + wrong-etype + bogus-bytes rejection). Limitations + Examples blocks updated. | `credentials/goldenticket/{validate.go,validate_test.go}` | this commit |

### P2.6 — Callstack spoof — hardware-breakpoint variant

| | Action | Where |
|---|---|---|
| [ ] | Implement the fortra-style HW-breakpoint-on-RET-gadget variant as a sibling primitive (`SpoofCallHWBP`) | `evasion/callstack/hwbp_*.go` |
| [ ] | Document the trade-off (more obfuscation, lower portability across DR scrub passes) | tech md |

### P2.7 — Preset stacks review

| | Action | Where | Done |
|---|---|---|---|
| [x] | Audit existing presets vs post-refactor maldev surface (sleepmask / callstack / cet / kcallback / hook / stealthopen all post-date the original presets) | `evasion/preset/` | this commit |
| [x] | Add a CET-aware Technique (`CETOptOut()`) — wraps `cet.Disable` with a graceful no-op when not enforced | `evasion/preset/preset_windows.go` | this commit |
| [x] | New `Hardened()` preset = AMSI + ETW + full ntdll unhook + CET opt-out (between Stealth and Aggressive — drops the irreversible per-process mitigations so callers can still inject afterwards) | `evasion/preset/preset_windows.go` | this commit |
| [x] | Refresh `Aggressive()` to include CETOptOut | same file | this commit |
| [x] | Stub parity for non-Windows | `evasion/preset/preset_stub.go` | this commit |
| [x] | Tests: TestHardened_NonEmpty_AndIncludesCET / TestCETOptOut_Apply_NoErrorWhenNotEnforced / TestAggressive_IncludesCET | `evasion/preset/preset_test.go` | this commit |
| [x] | Tech md update: 4-preset list + `CETOptOut` standalone helper | `docs/techniques/evasion/preset.md` | this commit |
| [x] | Re-test each preset E2E in the VM matrix | `cmd/examples/preset-suite/main.go` + `vmtest -bin -matrix windows` — Minimal (2) / Stealth (12) / Hardened (4) / Aggressive (6) + CETOptOut all apply cleanly in admin + lowuser | this commit |
| [x] | Future: callstack / sleepmask aren't one-shot Techniques (sleepmask is interactive Sleep, callstack supplies metadata to a pivot) — skipped intentionally; document the boundary in `evasion/preset/doc.go` if confusion arises. **Done**: doc.go gains a "What is NOT a preset Technique" section spelling out why each is absent (interactive Sleep cadence vs. apply-time, per-call metadata vs. process-wide setup). Also bumped the preset list to 4 (Minimal/Stealth/Hardened/Aggressive) since Hardened landed earlier and the doc.go was still listing only 3. | `evasion/preset/doc.go` | this commit |

### P2.8 — Thread pool — CET integration

| | Action | Where | Done |
|---|---|---|---|
| [x] | Future-proof helper `inject.ThreadPoolExecCET(shellcode)` — wraps with `cet.Wrap` when `cet.Enforced()`, forwards to `ThreadPoolExec`. No-op on current shipping builds (dispatcher not yet ENDBR64-required); zero-friction migration path the day Microsoft flips the bit. | `inject/threadpool_windows.go` | this commit |
| [x] | Tests: `TestThreadPoolExecCET_EmptyShellcode` + `TestThreadPoolExecCET_ZeroLenShellcode` (validation passthrough) | `inject/threadpool_test.go` | this commit |
| [x] | Tech md: new API Reference entry for `ThreadPoolExecCET` with rationale + Required-privileges + Platform fields | `docs/techniques/injection/thread-pool.md` | this commit |
| [ ] | Verify ENDBR64 enforcement on the pool dispatcher per Win build (research / E2E on Win11 24H2+) | live VM | — |

### P2.9 — Phantom DLL — explain + E2E

> The current API doesn't make sense to me: we inject the
> shellcode into the target's DLL first, then call `KernelCallbackExec`
> with both the shellcode AND the PID. Shouldn't we just pass the
> remote shellcode address?

| | Action | Where |
|---|---|---|
| [x] | Re-read the source + write a notes block explaining the two-step contract — `inject/phantomdll/notes.md` (new internal) walks through `PhantomDLLInject` (placement only — no execution trigger) vs `KernelCallbackExec` (self-contained allocate+write+WM_COPYDATA hijack), demonstrates that the naive combination duplicates shellcode, and outlines the `PhantomDLLPlace` / `KernelCallbackExecAt` decomposition required for genuine composability. The follow-up refactor row stays open pending operator input on the breaking change. | `inject/phantomdll/notes.md` (new internal) | this commit |
| [ ] | If the API is genuinely redundant, refactor: callers pass only the remote shellcode address | breaking change → semver bump |
| [ ] | Author E2E tests in the VM matrix to lock the behaviour | `inject/phantomdll/phantomdll_e2e_test.go` |

### P2.10 — Callback Execution — CET integration

| | Action | Where | Done |
|---|---|---|---|
| [x] | New `inject.MethodEnforcesCET(m CallbackMethod) bool` predicate (true for `CallbackRtlRegisterWait` + `CallbackNtNotifyChangeDirectory`) | `inject/callback_windows.go` | this commit |
| [x] | New `inject.ExecuteCallbackBytes(sc, method)` helper — auto-`cet.Wrap` when `MethodEnforcesCET ∧ cet.Enforced()`, allocates RW, copies bytes, flips RW→RX, calls `ExecuteCallback` | same file | this commit |
| [x] | Tests: `TestMethodEnforcesCET` (6-row table) + `TestExecuteCallbackBytes_NoCallback_AllocOnly` + `TestExecuteCallbackBytes_CETAffectedMethod` (intrusive-gated) | `inject/callback_test.go` | this commit |
| [x] | Tech md: IMPORTANT block now leads with the new helper, retains the manual `cet.Wrap` path for callers that want explicit control | `docs/techniques/injection/callback-execution.md` | this commit |

### P2.11 — RTCore64 — embed driver bytes + E2E

| | Action | Where | Done |
|---|---|---|---|
| [x] | Embed `RTCore64.sys` via `//go:embed` behind the `byovd_rtcore64` build tag (default builds keep `ErrDriverBytesMissing`); unit test verifies the PE header on the embedded blob | `kernel/driver/rtcore64/embed_byovd_rtcore64_windows.go` + matching `_test.go`; doc.go reword | next commit |
| [ ] | E2E test: install → ReadKernel → Uninstall on a HVCI-off VM | `kernel/driver/rtcore64/rtcore64_e2e_test.go` |  |

### P2.12 — Masquerade — preset binaries

| | Action | Where |
|---|---|---|
| [x] | Add cloned-resource presets for 13 identities × 2 UAC variants = 26 packages: notepad / cmd / svchost / taskmgr / explorer (System32 originals) + msedge / onedrive (v0.44.0) + acrobat / firefox / excel / sevenzip / vscode / claude (this commit). Generator at `pe/masquerade/internal/gen` uses a `{ID, Path}`-tuple slice with OS env-var expansion + per-identity SKIP-on-extract for inaccessible donors (wt.exe under WindowsApps DACL still skips on this dev box). Future contributor extends `identities` slice + runs the generator. | `pe/masquerade/preset/{acrobat,firefox,excel,sevenzip,vscode,claude}/{,admin}/` | this commit |
| [x] | Each preset bundles VERSIONINFO + icons + manifest + (optional) cert clone via `pe/cert` — bundled via the existing `masquerade.GenerateSyso` path the generator already calls (per-variant: AsInvoker base + RequireAdministrator admin). Optional cert clone NOT yet wired into the generator (operators can layer `cert.Copy` post-build). | preset structure | this commit |
| [x] | Light-weight unit test that the `.syso` builds — `TestPresetSysoBundles` walks every `pe/masquerade/preset/*` subdir, verifies the standard 3-file layout (`<pkg>_windows.go` + `<pkg>_stub.go` + `resource_windows_amd64.syso`) for both base and admin variants, and asserts each `.syso` carries the AMD64 COFF magic 0x8664 so `go build` will link it on Windows. Cross-platform — runs on Linux CI too. | `pe/masquerade/preset/preset_test.go` | this commit |

### P2.13 — PE Certificate Theft — pure-Go cert chain

| | Action | Where | Done |
|---|---|---|---|
| [x] | Recompute PE optional-header `CheckSum` after splice — `PatchPECheckSum` (MS `ImageHlp!CheckSumMappedFile` algorithm: 16-bit rolling-carry sum + size, CheckSum field masked to zero). Wired into `Strip` and `Write` so any cert-surgery output verifies cleanly. Tests: rejection of invalid PE, idempotency, MS verification-property round-trip. | `pe/cert/checksum.go` + `_test.go` | this commit |
| [x] | Pure-Go cert-chain forge — `Forge(opts ForgeOptions) (*ForgedChain, error)` builds a self-signed Leaf → optional Intermediate → self-signed Root chain via `crypto/x509` + `crypto/x509/pkix`, wraps the leaf signature into a PKCS#7 SignedData blob via `secDre4mer/pkcs7.NewSignedData + AddSignerChain + Finish`, then prepends the 8-byte WIN_CERTIFICATE header (Length / Revision 0x0200 / Type 0x0002 PKCS_SIGNED_DATA) padded to 8-byte alignment. Result drops straight into `cert.Write`. `ForgedChain` exposes per-cert + per-key artefacts so operators can re-use the chain across multiple PEs. Defaults: 2048-bit RSA, [now-1y, now+5y] validity, no intermediate. Sentinel `ErrInvalidForgeOptions`. 12 tests covering rejects-missing-subject, two-tier vs three-tier chain, self-signed root, validity defaults, WIN_CERT header layout (revision + type + length + 8-byte alignment), pkcs7.Parse round-trip carrying full chain, default-KeyBits-applied, and errors.Is contract. Limitations + Examples blocks updated honestly: forge produces a chain that fails Authenticode validation (signtool verify rejects, SmartScreen flags) — sufficient for file-properties cosmetic + "is signed" presence checks, NOT for real trust. | `pe/cert/{forge.go,forge_test.go}` | this commit |
| [x] | Tech md update — Limitations row rewritten (CheckSum no longer "stale", flagged as auto-recomputed via `PatchPECheckSum`); API Reference gains `PatchPECheckSum` + the `WriteVia` / `StripVia` / `ExportVia` triplet. | `docs/techniques/pe/certificate-theft.md` | this commit |

### P2.14 — DLL proxy — CheckSum + DOS stub (optional)

| | Action | Where | Done |
|---|---|---|---|
| [x] | `Options.PatchCheckSum bool` — when true, calls `pe/cert.PatchPECheckSum` after assembly. Reuses the algorithm shipped in P2.13 row 1. | `pe/dllproxy/dllproxy.go` | this commit |
| [x] | `Options.DOSStub bool` — when true, emits the canonical 128-byte MSVC DOS header + DOS program ("This program cannot be run in DOS mode.") with e_lfanew bumped from 0x40 to 0x80. Defenders fingerprinting on stub absence cannot distinguish from MSVC output. | `pe/dllproxy/dllproxy.go` | this commit |
| [x] | Tests: TestGenerate_DOSStub_EmbedsCanonicalProgram / _DefaultMinimalLayout / TestGenerate_PatchCheckSum_NonZeroChecksum / _NoPatchCheckSum_ZeroChecksum | `pe/dllproxy/dllproxy_test.go` | this commit |

### P2.15 — Service — service-account override

| | Action | Where | Done |
|---|---|---|---|
| [x] | Add `Config.Account` + `Config.Password` fields, propagate to `mgr.CreateService` via `mgr.Config.ServiceStartName` + `Password`. Empty Account preserves the existing LocalSystem default — fully backwards-compatible. | `persistence/service/service_windows.go` | this commit |
| [x] | Tests: `TestConfig_AccountFieldsTypeCheck` (unit) + `TestInstall_BuiltinAccount_PromotesToAdmin` (intrusive-gated, uses NT AUTHORITY\\NetworkService — no password, no LSA grant needed) | `persistence/service/service_test.go` | this commit |
| [x] | Tech md — Config table gains `Account` / `Password` rows + IMPORTANT note about `SeServiceLogonRight` | `docs/techniques/persistence/service.md` | this commit |
| [x] | `GrantSeServiceLogonRight(account)` — wraps `LookupSID` + `LsaOpenPolicy(POLICY_CREATE_ACCOUNT \| POLICY_LOOKUP_NAMES)` + `LsaAddAccountRights` + `LsaClose`. Idempotent. NTSTATUS errors translated through `LsaNtStatusToWinError`. New const `SeServiceLogonRight` + sentinel `ErrLsaCallFailed`. Tests: TestGrantSeServiceLogonRight_RejectsEmptyAccount (host) + TestGrantSeServiceLogonRight_OnLocalService (intrusive, gated on user.IsAdmin). doc.go + tech md IMPORTANT block / Limitations rewritten — no longer queued, callable today. | `persistence/service/lsa_grant_windows.go` (new) | this commit |

### P2.16 — LNK — fileless + stealthopen

| | Action | Where | Done |
|---|---|---|---|
| [x] | Add `BuildBytes() ([]byte, error)` (zero-disk via IShellLinkW + IPersistStream::Save on HGLOBAL IStream) and `WriteTo(io.Writer)` for in-memory hand-off | `persistence/lnk/lnk_buildbytes_windows.go` (NEW) | f77187a |
| [x] | Add `WriteVia(creator stealthopen.Creator, path string) error` — lands bytes via the operator-controlled write primitive. Required new write-side parallel `Creator` interface in `evasion/stealthopen` (`Create(path) (io.WriteCloser, error)` + `*StandardCreator` + `UseCreator`) | `persistence/lnk/lnk_buildbytes_windows.go`, `evasion/stealthopen/opener.go` | this commit |
| [x] | Tech md updates — three sinks (`Save` / `BuildBytes` / `WriteVia`) documented, second sequence diagram for the IShellLinkW path, "Stealth landing" example using `stealthopen.Creator`, "Zero-disk C2 staging" example using `BuildBytes` / `WriteTo` | `docs/techniques/persistence/lnk.md` | this commit |

### P2.17 — Session — alternate desktop / station

| | Action | Where | Done |
|---|---|---|---|
| [x] | Document hard-coded default-desktop assumption — Limitations bullet rewritten to spell out the inherit-from-caller default and the new override path. New "Advanced — alternate desktop / station" Example covers SYSTEM service station + hidden-desktop scenarios. | `docs/techniques/process/session.md` | this commit |
| [x] | New `Options{Desktop string}` + `CreateProcessOnActiveSessionsWith(token, exe, args, opts)` — populates `STARTUPINFOW.lpDesktop` when set; legacy `CreateProcessOnActiveSessions` delegates with a zero `Options`. Tests: TestOptions_DesktopFieldTypeCheck (compile-time guard) + TestCreateProcessOnActiveSessionsWith_NilOptionsLegacy (signature drift sentinel). | `process/session/session_windows.go` | this commit |

### P2.18 — HideProcess — broader patches

| | Action | Where |
|---|---|---|
| [-] | ~~Add patches for WMI Win32_Process query path (cimwin32.dll!QueryProcesses)~~ — **descoped**: cimwin32.dll only loads in WMI provider hosts (`wmiprvse.exe`), so the patch needs a separate cross-process injection target than the `PatchAll` victim. Limitation now documented in `docs/techniques/process/hideprocess.md` so callers know to handle WMI externally. | tech md only | this commit |
| [x] | Add patches for `EnumProcesses` / `Process32Next` — `PatchEnumProcesses` (kernel32!K32EnumProcesses → `xor eax, eax; ret`), `PatchToolhelp` (Process32FirstW + Process32NextW → same patch), `PatchAll` (NQSI + EnumProcesses + Toolhelp). Refactored `PatchProcessMonitor` to share the open+resolve+write helper. New `boolFalsePatch` byte sequence + 4 invalid-PID tests + 1 byte regression test. | `process/tamper/hideprocess/{hideprocess_windows,hideprocess_stub,hideprocess_windows_test}.go` + tech md | this commit |
| [x] | Tech md: cover-paths matrix — 5-row table mapping each enumeration path (NQSI, psapi EnumProcesses, Toolhelp32, WMI Win32_Process, kernel-source) to the Patch* function that covers it (or "not covered" with the structural reason). | `docs/techniques/process/hideprocess.md` | this commit |

### P2.19 — Phant0m — heartbeat re-kill

| | Action | Where | Done |
|---|---|---|---|
| [x] | Add `Heartbeat(ctx, interval, caller) error` — first Kill is synchronous (returns its error); subsequent Kills run on a ticker until ctx cancellation. Best-effort retry on transient errors. | `process/tamper/phant0m/phant0m_windows.go` | this commit |
| [x] | Tests: `TestHeartbeat_RejectsNonPositiveInterval` (unit) + `TestHeartbeat_RespectsContextCancellation` (intrusive-gated) | `process/tamper/phant0m/phant0m_test.go` | this commit |
| [x] | Tech md update — Advanced example replaces the manual `for range time.Tick(...)` loop with `go phant0m.Heartbeat(ctx, …)` + deferred context cancel | `docs/techniques/process/phant0m.md` | this commit |
| [x] | Use `I_QueryTagInformation` when present to target only EventLog threads (instead of nuking every thread in the host svchost) — already implemented inline in `process/tamper/phant0m/phant0m_windows.go` (functions `canQueryTags` + `isEventLogThread`, called from the per-TID loop in `Kill`). The row's suggested `eventlog_tag.go` split was advisory; keeping the logic next to `Kill` is clearer at the current size (~80 LOC). Graceful fallback: when `I_QueryTagInformation` isn't present, every thread is treated as an EventLog worker (preserves the original behaviour on hosts that don't expose the undocumented advapi32 export). | `process/tamper/phant0m/phant0m_windows.go` | already done — backlog row was stale |

### P2.20 — Sandbox detection — scoring algorithm

| | Action | Where | Done |
|---|---|---|---|
| [x] | Add `Score(results []Result) int` — capped 0..100 aggregate over `CheckAll` output. Coexists with `IsSandboxed` (binary verdict) so callers pick whichever fits. | `recon/sandbox/sandbox.go` | this commit |
| [x] | Add `Weights() map[string]int` — returns a copy so operators can audit / mutate without touching package state | same file | this commit |
| [x] | Per-check weights table — `debugger=20`, `vm=18`, `domain=15`, `process=13`, `username=12`, `hostname=12`, `process-count=7`, `connectivity=6`, `ram=5`, `disk=5`, `cpu=3` (sum=116, capped at 100) | same file | this commit |
| [x] | Tests: ZeroOnEmpty + StrongSignals (3 cases) + Capped100 + UnknownNamesContributeZero + WeightsReturnsCopy | `recon/sandbox/sandbox_test.go` | this commit |
| [x] | Tech md: API Reference rows + scoring-weights table + new "Advanced — score-based bail" Example + Limitations updated (false-positives wording + static-weights caveat) | `docs/techniques/recon/sandbox.md` | this commit |

### P2.21 — Drive enumeration — event-driven path

| | Action | Where | Done |
|---|---|---|---|
| [x] | Add `(*Watcher).WatchEvents(buffer int) (<-chan Event, error)` — message-only window subscribed to `WM_DEVICECHANGE`; ms-latency on `DBT_DEVICEARRIVAL` / `DBT_DEVICEREMOVECOMPLETE`; clean `WM_CLOSE → WM_DESTROY → WM_QUIT` shutdown on ctx cancel | `recon/drive/watch_event_windows.go` (NEW) | this commit |
| [x] | Wire 9 user32 procs (`RegisterClassExW`, `UnregisterClassW`, `CreateWindowExW`, `DestroyWindow`, `DefWindowProcW`, `GetMessageW`, `DispatchMessageW`, `PostMessageW`, `PostQuitMessage`) | `win/api/dll_windows.go` | this commit |
| [x] | Keep `(*Watcher).Watch(interval)` (polling) untouched — non-breaking; both watchers coexist | unchanged | this commit |
| [x] | New sentinel `ErrEventPumpFailed` for class-register / window-create failures | `recon/drive/watch_event_windows.go` | this commit |
| [x] | Tests: `TestErrEventPumpFailed_Exported` (regression guard) + `TestWatcher_WatchEvents_StartsAndCancels` (full startup + ctx-cancel teardown round-trip) | `recon/drive/drive_test.go` | this commit |
| [x] | Tech md update — fielded API Reference entry for `WatchEvents` + "When to pick which" decision table | `docs/techniques/recon/drive.md` | this commit |

### P2.22 — Special folder — KNOWNFOLDERID upgrade

| | Action | Where | Done |
|---|---|---|---|
| [x] | `SHGetKnownFolderPath` wrapper + 14 `FOLDERID_*` GUIDs + `KnownFolderFlag` enum + `ErrKnownFolderNotFound` sentinel; CoTaskMemFree handled internally | `recon/folder/known_windows.go` | this commit |
| [x] | `Ole32` LazyDLL + `ProcCoTaskMemFree` + `ProcSHGetKnownFolderPath` exported under `win/api` | `win/api/dll_windows.go` | this commit |
| [x] | Tests: 13 GUIDs auto-resolve via `KFF_DONT_VERIFY`; `KFF_CREATE` smoke; bogus-GUID returns `ErrKnownFolderNotFound` | `recon/folder/known_windows_test.go` | this commit |
| [x] | doc.go reword: dual entry points (legacy CSIDL `Get` + modern `GetKnown`); recommend KNOWNFOLDERID for new code | `recon/folder/doc.go` | this commit |
| [x] | Tech md gains a fielded API Reference for both `GetKnown` and `Get` (with Required-privileges + Platform fields), modern + legacy "Simple" examples | `docs/techniques/recon/folder.md` | this commit |
| [x] | Migrate internal callers — `credentials/lsassdump`, `inject/phantomdll`, `persistence/startup` switched from `folder.Get(CSIDL_*)` / `os.UserConfigDir` to `folder.GetKnown(windows.FOLDERID_*)`. Old `folder.Get(CSIDL_*)` API kept as legacy entry. | repo-wide sweep | this commit |

### P2.23 — BOF Loader — feature parity

| | Action | Where |
|---|---|---|
| [~] | Implement Beacon-API stubs (BeaconOutput, BeaconFormatAlloc, BeaconErrorD, BeaconGetSpawnTo, …) routed to user-supplied callbacks. Now implemented: `BeaconPrintf` (verbatim — varargs unfeasible from a `syscall.NewCallback` thunk), `BeaconOutput`, `BeaconDataParse`/`Int`/`Short`/`Length`/`Extract`, `BeaconFormatAlloc`/`Reset`/`Free`/`Append`/`Int`/`ToString`. The Format family backs each formatp pointer with a Go-allocated slice held in a package-level map so Go GC doesn't reclaim it; cursor + size live inside the BOF-allocated formatp struct (same wire shape as datap). `BeaconFormatInt` writes big-endian (CS network-byte-order convention). Output is plumbed through `BOF.Execute([]byte) → ([]byte, error)` (was returning `nil, nil` previously). Concurrency: BOF execution is serialised via `bofMu`. Symbol resolution at relocation time falls through to `resolveBeaconImport`; unresolved Beacon names error out clean instead of the previous silent NULL-patching. **Not yet:** `BeaconFormatPrintf` (vararg constraint), `BeaconErrorD`/`DD`/`NA`, `BeaconGetSpawnTo`. Tests: 11 unit tests covering each stub + nil-receiver guards + roundtrip parser + format-buffer truncation + reset/free observability. | `runtime/bof/beacon_api_windows.go` (new) + `runtime/bof/bof_windows.go` + `runtime/bof/beacon_api_windows_test.go` (new) + tech md Limitations | this + previous commits |
| [x] | Add ADDR32, REL64, IMAGE_REL_AMD64_ADDR32 variants to the relocation switch — added `IMAGE_REL_AMD64_ABSOLUTE` (no-op), `_ADDR32` (with high-32-bits guard so overflow errors loud instead of corrupting the BOF), and the `_REL32_1` through `_REL32_5` bias variants. The row title's "REL64" doesn't exist in the AMD64 COFF spec — `_ADDR64` is already supported. Unsupported types still error with a clear message. Existing patches for `_ADDR64`/`_ADDR32NB`/`_REL32` unchanged. | `runtime/bof/bof_windows.go` (kept inline, no separate `reloc.go` file) + tech md Limitations + `TestRelocationConstants` regression guard | this commit |
| [x] | External-symbol resolution via `win/api.ResolveByHash` — `parseDollarImport` + `resolveBeaconImport` fall through to `api.ResolveByHash(hash.ROR13Module(dll), hash.ROR13(fn))` for `__imp_<DLL>$<Func>` form CS-style imports. PEB walk + ROR13 export-table match — no `GetProcAddress`/`LoadLibraryA` in the API trail. Unresolved symbols error out at relocation time. Tests: `TestParseDollarImport` (8 cases: KERNEL32 upper-cased, USER32.DLL preserved, malformed inputs rejected) + `TestResolveBeaconImport_DollarImport` (end-to-end resolves `__imp_KERNEL32$LoadLibraryA` to a non-zero address on win10). | `runtime/bof/beacon_api_windows.go` + test file (kept inline, no separate `resolver.go` file) | commit `0bfca00` |
| [~] | Tech md update + example BOFs — tech md updated across the previous P2.23 commits (Limitations now lists Beacon-API coverage, dollar-import resolver, full relocation table, partial-Beacon-API-surface caveat). `runtime/bof/testdata/` gains C sources for three example BOFs (`hello_beacon.c` smoke-tests `BeaconPrintf`; `parse_args.c` exercises `BeaconDataParse`/`Int`/`Extract`; `loadlib.c` exercises the dollar-import path through `kernel32!LoadLibraryA`/`FreeLibrary`) plus a `README.md` documenting the build pipeline. **Pending:** the `.o` artefacts themselves require `mingw-w64` cross-compilation which is not on the dev box — sources are in place, build commands documented, future contributor with mingw drops the `.o` next to existing `nop.o`/`whoami.o` in `testutil/` and adds the loader integration tests. | `runtime/bof/testdata/{README.md,hello_beacon.c,parse_args.c,loadlib.c}` (new) | this commit |

### P2.24 — API hashing — algorithm extensions

| | Action | Where | Done |
|---|---|---|---|
| [x] | Add JenkinsOAAT, FNV-1a (32 + 64), DJB2, CRC32 alongside ROR13 — pure-Go single-pass byte hashes; no allocation; no syscall | `hash/apihash.go` | this commit |
| [x] | Tests with hand-computed reference vectors for `kernel32.dll` / `LoadLibraryA` / empty string + collision-distinctness sanity guard | `hash/apihash_test.go` | this commit |
| [x] | Tech md update — table comparing the 6 algorithms + composition example with `wsyscall.NewHashGateWith` / `WithHashFunc` + Limitation reflecting non-ROR13 fingerprint resilience | `docs/techniques/syscalls/api-hashing.md` | this commit |
| [x] | `cmd/hashgen` CLI — emits `const HashRor13LoadLibraryA = 0xec0e4e8e` style declarations for any of the 7 algorithms (ror13 / ror13module / fnv1a32 / fnv1a64 / jenkins / djb2 / crc32). Names from positional args or `-stdin` (one per line, `#` comments). Output to `-o` or stdout, optional `-package` declaration, configurable `-comment` header. Designed for `go generate` — emit once, commit, ship without runtime hashing cost. Tests: TestIdentifier, TestPickAlgo_KnownNames / _RejectsUnknown / _ProducesNonZeroForReferenceVectors. | `cmd/hashgen/main.go` (new) | this commit |
| [x] | Audit consumers (`inject/*`, `evasion/*`) for plumbing that should accept a `HashFunc` instead of hard-coding ROR13. **Outcome:** no consumer plumbing changes needed — `inject/*` / `evasion/*` route through `*wsyscall.Caller` already, which honors `WithHashFunc(fn)`. The audit surfaced a real drift inside `win/syscall` itself: `HashGateResolver.Resolve` and `Caller.CallByHash` both used the hardcoded ROR13Module hash `0x411677B7` to find ntdll regardless of `HashFunc`, leaving the fingerprint constant in every non-ROR13 build. Fixed by threading a per-instance `ntdllHash` (computed via `fn("ntdll.dll")` at constructor time) through both surfaces; new `pebModuleByHashFunc` walks the PEB hashing each module name with the supplied fn. | `win/syscall/{pebwalk,resolver,syscall,direct}_windows.go` + tech md Limitations + regression test | this commit |

### P2.25 — Syscall package — clearer separation in docs

| | Action | Where | Done |
|---|---|---|---|
| [x] | Add "Three concerns (read this first)" section to `syscalls/README.md` — Mermaid showing the 3-axis composition + table mapping each axis to its sub-page + 3 example combinations | `docs/techniques/syscalls/README.md` | this commit |
| [x] | "What api-hashing is NOT" disambiguation block at the top of `api-hashing.md` | `docs/techniques/syscalls/api-hashing.md` | this commit |
| [x] | "What direct/indirect is NOT" disambiguation block at the top of `direct-indirect.md` | `docs/techniques/syscalls/direct-indirect.md` | this commit |
| [x] | "What SSN resolvers are NOT" disambiguation block at the top of `ssn-resolvers.md` | `docs/techniques/syscalls/ssn-resolvers.md` | this commit |

---

## P3 — Ideas / new packages

### P3.1 — Custom packer

| | Action | Where |
|---|---|---|
| [x] | Brainstorm scope: what does maldev's packer add over UPX-morph? Design doc covers gap analysis vs morph/srdi/cert-graft, threat model, hard constraints (pure Go toolchain, polymorphic stub via compile-time templating, library + CLI surface, cross-platform Windows+Linux), capability matrix (cipher / compressor / key location / anti-debug / AMSI silence / cert graft / multi-target — all opt-in), 3-phase implementation plan. | `.dev/refactor-2026/packer-design.md` | this commit |
| [x] | Phase 1 shipped as **v0.61.0** (Phase 1e UPX-style in-place transform). PE32+ and ELF64 supported via `pe/packer/transform/{pe,elf}.go`: parse input, encrypt `.text` with SGN polymorphic encoder, append CALL+POP+ADD-prologue decoder stub as a new section, rewrite OEP. Single-binary output; kernel does the loading. E2E ship gate (`TestPackBinary_LinuxELF_E2E`) runs a packed Go static-PIE fixture to clean exit. KNOWN-ISSUES-1e closed; six bugs resolved. | `pe/packer/{transform,stubgen,packer.go}` | this commit |
| [ ] | Phase 2: section-shuffle + import-table scramble. Partial cover ships today: `pe/morph.UPXMorph` renames sections, `pe/strip` removes residue. True shuffle + IAT-by-hash resolver against the packed output is the remaining work. | `pe/packer/scramble.go` (new) |
| [~] | Phase 3 partial: junk sections shipped for both PE and ELF. PE side: `AddCoverPE` appends new `MEM_READ` sections (no W, no X). ELF side: `AddCoverELF` appends new `PF_R` PT_LOADs. Both share `JunkSection` + `CoverOptions` + 3 `JunkFill` strategies (Random / Zero / Pattern with frequency-ordered byte alphabet). 9 tests across both formats: empty-options + non-format-magic rejects, debug/pe + debug/elf round-trips with named sections / counted PT_LOADs, all-three-fill smoke, original .text body byte-identical, ELF Ehdr preserved except `e_phnum`. **Limitations:** ELF cover requires PHT slack between the table end and the first PT_LOAD's file offset; Go static-PIE binaries place the first PT_LOAD at offset 0 → `ErrCoverSectionTableFull` (regression-tested via `TestAddCoverELF_RejectsNoSlack`). **Still pending v2:** fake imports (Import Directory + IAT scaffolding pointing at benign DLLs) + PHT relocation for slack-less ELFs. | `pe/packer/{cover.go,cover_test.go,cover_elf.go,cover_elf_test.go}` (new) | this + `1cc3237` |

### P3.2 — Red Pill family — IDT / GDT / LDT VM detection

| | Action | Where | Done |
|---|---|---|---|
| [x] | `SIDT` / `SGDT` / `SLDT` user-mode wrappers: 3-byte raw-encoded Plan 9 instructions (`0F 01 0F`, `0F 01 07`, `0F 00 07`), Go helpers fill a 10-byte (SIDT/SGDT) or 2-byte (SLDT) buffer and return `(base uint64, limit uint16)`. | `recon/antivm/{redpill_amd64.s,redpill_amd64.go,redpill_other.go}` | this commit |
| [x] | Aggregator `Probe() RedpillReport` returns IDT/GDT bases + LDT selector + per-source `Suspect` flags + composite `LikelyVM`. The IDT/GDT flags fire when the base falls outside the canonical kernel half (`0xffff8000_00000000` upward); LDT flag fires on any non-zero selector. | `recon/antivm/redpill.go` | this commit |
| [x] | Tests: `TestSIDT_AMD64ReturnsNonZero` / `_SGDT_` / `_SLDT_` invariant guards + `TestProbe_ReportShape` (LikelyVM consistency) + `TestProbe_StableAcrossCalls` (no drift between consecutive reads). Cross-OS: stub returns zero report on non-amd64. | `recon/antivm/redpill_test.go` | this commit |
| [x] | Tech md: fielded API Reference for SIDT/SGDT/SLDT + Probe with godoc anchors, Rutkowska 2004 Red Pill citation in the prose, modern-HVM obsolescence call-out, OPSEC + UMIP `#GP` caveat documented. | `docs/techniques/recon/anti-analysis.md` API Reference | this commit |

### P3.3 — CPUID / RDTSC / hypervisor-leaf VM detection

| | Action | Where | Done |
|---|---|---|---|
| [x] | Read `CPUID.1:ECX[31]` (hypervisor-present bit) — `HypervisorPresent()` issues `CPUID` directly via `cpuidRaw` (Go-asm `cpuid_amd64.s`) and returns `ECX[31]`. Stub on non-amd64 returns `false`. Cross-platform on amd64 (Windows + Linux + macOS). | `recon/antivm/{cpuid_amd64.s,hypervisor_amd64.go,hypervisor_other.go}` | this commit |
| [x] | Read `CPUID.40000000h` vendor string — `HypervisorVendor()` returns the 12-byte `EBX:ECX:EDX` blob; `HypervisorVendorName(sig)` maps it to a friendly label across 11 known hypervisors (VMware / KVM / Hyper-V / Xen / QEMU-TCG / VirtualBox / bhyve / Parallels x2 / ACRN / QNX). 6 tests covering asm linkage, table coverage, unknown-empty contract, and Present↔Vendor consistency. | `recon/antivm/{hypervisor.go,hypervisor_amd64.go,hypervisor_other.go,hypervisor_test.go,hypervisor_amd64_test.go}` | this commit |
| [x] | Implement RDTSC timing attack on a known-fast instruction sequence (CPUID inside VMENTER will spike) — `RDTSCDelta(samples)` returns the median of N CPUID-bracketed RDTSC reads (asm in `rdtsc_amd64.s`); `LikelyVirtualizedByTiming(threshold)` is the boolean predicate. `DefaultRDTSCThreshold = 1000` separates bare-metal (~30-50 cycles) from HVM (~500-3000+ cycles). Stub on non-amd64 returns 0. | `recon/antivm/{rdtsc_amd64.s,hypervisor_amd64.go,hypervisor_other.go}` | this commit |
| [x] | Author a single `recon/antivm/Hypervisor()` that aggregates results — `Hypervisor()` returns a `HypervisorReport{Present, VendorSig, VendorName, TimingDelta, LikelyVM}`; `LikelyVM` is the OR of every positive signal. Convenience getter `(r).Detected() bool`. Cross-platform — calls degrade through each underlying primitive's stub on non-amd64. Tests: TestRDTSCDelta_NonPositiveSamplesReturnZero / _ReturnsPositiveOnAmd64, TestLikelyVirtualizedByTiming_ThresholdHonoured, TestHypervisor_ReportShape (invariants) / _StableAcrossCalls. | `recon/antivm/hypervisor.go` | this commit |

### P3.4 — VMware I/O port detection

| | Action | Where |
|---|---|---|
| [x] | `BackdoorVMware` shipped — Plan 9 asm in `recon/antivm/vmware_amd64.s` issues `IN EAX, DX` against port 0x5658 with EAX="VMXh" + ECX=CMD_GETVERSION; on VMware the hypervisor traps the instruction and EBX echoes "VMXh". High-level wrapper `BackdoorVMware()` gates on `hasPortIOPrivileges` (Linux: `iopl(3)` syscall + euid==0; Windows: always false because user-mode port I/O is impossible regardless of token elevation) and returns `ErrBackdoorPrivilege` instead of running the IN at CPL 3 where it would `SIGSEGV`/`#GP`. Tests cover constants pinning, zero-value shape, and the privilege-gate sentinel. | `recon/antivm/vmware_amd64.{s,go}` + `vmware_{linux,windows,other_os}.go` + `vmware_test.go` | this commit |
| [x] | Tech md updated — new Limitations bullet (`recon/antivm` Ring 0 / iopl requirement) + new Examples block (privileged BackdoorVMware path paired with user-mode `Hypervisor()` for tier-1/tier-2 detection). | `docs/techniques/recon/anti-analysis.md` | this commit |

### P3.5 — Refresh worked examples

The `docs/examples/*.md` were authored before the recent
maldev changes (cet, MethodIndirectAsm, dllproxy, gadget pool,
swappable HashFunc). Re-write them so they reflect the current
state of the art.

| | Action | Where |
|---|---|---|
| [x] | Rewrite `docs/examples/basic-implant.md` using `MethodIndirectAsm` + custom HashFunc — example now showcases the full post-refactor stack: `antivm.Hypervisor()` sandbox bail (P3.3), `MethodIndirectAsm` + `NewHashGateWith(hash.FNV1a32)` to defeat ROR13 fingerprinting, `evasion.preset.Stealth()` instead of hand-listed AMSI/ETW/Unhook, `crypto.UseDecrypted` for defer-wiped plaintext, `inject.Build()` builder + `SelfInjector.InjectedRegion` for `(Addr, Size)` hand-off to `sleepmask.Mask`, and a `cmd/hashgen` build-time invocation that pre-computes the FNV-1a constants so the binary carries no plaintext API names AND no ROR13-fingerprintable hashes. New "What changed" header documents each migration; new "Hardening dials" footer shows three swap-points (`preset.Hardened()` for CET, `MethodEarlyBirdAPC` for quieter execution, `crypto.NewAESGCMReader` for multi-MB streaming). | `docs/examples/basic-implant.md` | this commit |
| [x] | Rewrite `docs/examples/evasive-injection.md` with `cet.Marker`/`cet.Wrap` + `inject.ExecuteCallbackBytes` + `evasion/preset.Stealth`/`CETOptOut` + `recon/antivm.Hypervisor` sandbox-bail. Two code paths shipped: (1) `preset.Stealth + ExecuteCallbackBytes` self-process, (2) MultiStealth-aware manual unhook for callers who want the path-based file hook to fire only once per file. Cross-process Build + decorator chain section corrected (`.Use`/`.Create`/`MethodCreateRemoteThread`; the standalone `SectionMapInject` / `PhantomDLLInject(opener)` / `ModuleStomp` calls live in a sibling block since they aren't in the builder's fallback graph). Technique comparison extended with a CET-sensitive column. | `docs/examples/evasive-injection.md` | this commit |
| [x] | Rewrite `docs/examples/full-chain.md` to chain: payload encrypt → masquerade preset → inject (indirect-asm) → preset evasion → sleepmask → cleanup. Restructured into a build-time `pe/masquerade.Build` step that emits `.syso` + a runtime main() with 7 numbered phases (Recon two-tier bail via `Hypervisor()` then `IsSandboxed`; Evasion via `preset.Stealth + CETOptOut` through `MethodIndirectAsm + FNV-1a HashGate`; Inject via `crypto.UseDecrypted` + `inject.SectionMapInject`; Sleep loop via `sleepmask.Mask.Sleep` Ekko strategy with cross-process RemoteMask caveat; C2 via uTLS Chrome JA3 + SNI; Post-ex token theft + EnableAllPrivileges; Cleanup `timestomp.SetFull` + `selfdelete.Run`). New "Hardening dials" footer documents three swap-points (preset.Hardened, MethodIndirect for ARM64, RC4Cipher for non-AES-NI). Three Mermaid diagrams (build→runtime overview, OPSEC layers, phase summary table). | `docs/examples/full-chain.md` | this commit |
| [x] | Add a `docs/examples/dllproxy-side-load.md` (NEW) — generate a forwarder DLL, drop it next to a vulnerable signed exe. End-to-end chain across 4 packages: `recon/dllhijack.ScanAll + Rank` for discovery, `pe/parse.OpenFile.ExportEntries` for the named-export list, `pe/dllproxy.GenerateExt` for the forwarder PE emit (PayloadDLL stub + DOSStub + PatchCheckSum), `stealthopen.WriteAll` for the drop, `timestomp.CopyFrom` for blend-in. Validate + canary preflight via `dllhijack.Validate`. Per-Kind trigger table. Limitations cover KnownDLLs exclusion, same-arch only, MAX_PATH cap on forwarder strings, signed-loader-policy gates, mtime forensic indicator, KindProcess reload reachability. Closes P3.5. | `docs/examples/dllproxy-side-load.md` | this commit |

---

## Tracking template

When ticking a row:

1. Add the commit SHA in a **Done** column you append to the row, e.g.
   `| [x] | … | `commit-sha` |`
2. Bump `last_reviewed` + `reflects_commit` in the front-matter.
3. If a whole P-section completes, add a one-line summary at the
   top of that section.

When **adding** a row, slot it numerically (`P1.10`, `P2.26`, …)
so cross-references in commit messages don't shift.
