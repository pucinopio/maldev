// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/google/uuid"
	"github.com/oioio-space/maldev/internal/manager/store/ent/issuer"
)

// Issuer is the model entity for the Issuer schema.
type Issuer struct {
	config `json:"-"`
	// ID of the ent.
	ID uuid.UUID `json:"id,omitempty"`
	// Name holds the value of the "name" field.
	Name string `json:"name,omitempty"`
	// KeyID holds the value of the "key_id" field.
	KeyID string `json:"key_id,omitempty"`
	// PublicKey holds the value of the "public_key" field.
	PublicKey []byte `json:"public_key,omitempty"`
	// EncryptedPriv holds the value of the "encrypted_priv" field.
	EncryptedPriv []byte `json:"encrypted_priv,omitempty"`
	// Active holds the value of the "active" field.
	Active bool `json:"active,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// RetiredAt holds the value of the "retired_at" field.
	RetiredAt *time.Time `json:"retired_at,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the IssuerQuery when eager-loading is set.
	Edges        IssuerEdges `json:"edges"`
	selectValues sql.SelectValues
}

// IssuerEdges holds the relations/edges for other nodes in the graph.
type IssuerEdges struct {
	// Licenses holds the value of the licenses edge.
	Licenses []*License `json:"licenses,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// LicensesOrErr returns the Licenses value or an error if the edge
// was not loaded in eager-loading.
func (e IssuerEdges) LicensesOrErr() ([]*License, error) {
	if e.loadedTypes[0] {
		return e.Licenses, nil
	}
	return nil, &NotLoadedError{edge: "licenses"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*Issuer) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case issuer.FieldPublicKey, issuer.FieldEncryptedPriv:
			values[i] = new([]byte)
		case issuer.FieldActive:
			values[i] = new(sql.NullBool)
		case issuer.FieldName, issuer.FieldKeyID:
			values[i] = new(sql.NullString)
		case issuer.FieldCreatedAt, issuer.FieldRetiredAt:
			values[i] = new(sql.NullTime)
		case issuer.FieldID:
			values[i] = new(uuid.UUID)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the Issuer fields.
func (_m *Issuer) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case issuer.FieldID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				_m.ID = *value
			}
		case issuer.FieldName:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field name", values[i])
			} else if value.Valid {
				_m.Name = value.String
			}
		case issuer.FieldKeyID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field key_id", values[i])
			} else if value.Valid {
				_m.KeyID = value.String
			}
		case issuer.FieldPublicKey:
			if value, ok := values[i].(*[]byte); !ok {
				return fmt.Errorf("unexpected type %T for field public_key", values[i])
			} else if value != nil {
				_m.PublicKey = *value
			}
		case issuer.FieldEncryptedPriv:
			if value, ok := values[i].(*[]byte); !ok {
				return fmt.Errorf("unexpected type %T for field encrypted_priv", values[i])
			} else if value != nil {
				_m.EncryptedPriv = *value
			}
		case issuer.FieldActive:
			if value, ok := values[i].(*sql.NullBool); !ok {
				return fmt.Errorf("unexpected type %T for field active", values[i])
			} else if value.Valid {
				_m.Active = value.Bool
			}
		case issuer.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				_m.CreatedAt = value.Time
			}
		case issuer.FieldRetiredAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field retired_at", values[i])
			} else if value.Valid {
				_m.RetiredAt = new(time.Time)
				*_m.RetiredAt = value.Time
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the Issuer.
// This includes values selected through modifiers, order, etc.
func (_m *Issuer) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryLicenses queries the "licenses" edge of the Issuer entity.
func (_m *Issuer) QueryLicenses() *LicenseQuery {
	return NewIssuerClient(_m.config).QueryLicenses(_m)
}

// Update returns a builder for updating this Issuer.
// Note that you need to call Issuer.Unwrap() before calling this method if this Issuer
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *Issuer) Update() *IssuerUpdateOne {
	return NewIssuerClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the Issuer entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *Issuer) Unwrap() *Issuer {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: Issuer is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *Issuer) String() string {
	var builder strings.Builder
	builder.WriteString("Issuer(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("name=")
	builder.WriteString(_m.Name)
	builder.WriteString(", ")
	builder.WriteString("key_id=")
	builder.WriteString(_m.KeyID)
	builder.WriteString(", ")
	builder.WriteString("public_key=")
	builder.WriteString(fmt.Sprintf("%v", _m.PublicKey))
	builder.WriteString(", ")
	builder.WriteString("encrypted_priv=")
	builder.WriteString(fmt.Sprintf("%v", _m.EncryptedPriv))
	builder.WriteString(", ")
	builder.WriteString("active=")
	builder.WriteString(fmt.Sprintf("%v", _m.Active))
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(_m.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	if v := _m.RetiredAt; v != nil {
		builder.WriteString("retired_at=")
		builder.WriteString(v.Format(time.ANSIC))
	}
	builder.WriteByte(')')
	return builder.String()
}

// Issuers is a parsable slice of Issuer.
type Issuers []*Issuer
